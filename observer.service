# =============================================================================
# Observer Intelligence Platform - Systemd Service File
# Version: 1.0.0
# Last Updated: 2026-02-02
# Authors: Mr Cat + Claude AI
# =============================================================================
#
# INSTALLATION:
#   1. Copy this file to systemd directory:
#      sudo cp observer.service /etc/systemd/system/
#
#   2. Create system user (optional, recommended for security):
#      sudo useradd --system --no-create-home --shell /bin/false observer
#
#   3. Set ownership of Observer directory:
#      sudo chown -R observer:observer /opt/observer
#
#   4. Reload systemd and enable service:
#      sudo systemctl daemon-reload
#      sudo systemctl enable observer
#      sudo systemctl start observer
#
# MANAGEMENT:
#   sudo systemctl status observer    # Check status
#   sudo systemctl start observer     # Start service
#   sudo systemctl stop observer      # Stop service
#   sudo systemctl restart observer   # Restart service
#   sudo journalctl -u observer -f    # View logs
#
# =============================================================================

[Unit]
Description=Observer Intelligence Platform
Documentation=https://github.com/Rybatter50-cloud/Observer
After=network.target network-online.target
Wants=network-online.target

[Service]
# -----------------------------------------------------------------------------
# Service Identity
# -----------------------------------------------------------------------------
Type=simple

# Run as dedicated user (create with: useradd --system observer)
# For development, you can use your own user
User=observer
Group=observer

# -----------------------------------------------------------------------------
# Working Directory & Environment
# -----------------------------------------------------------------------------
WorkingDirectory=/opt/observer
EnvironmentFile=/opt/observer/.env

# -----------------------------------------------------------------------------
# Execution
# -----------------------------------------------------------------------------
# Using virtual environment (recommended)
ExecStart=/opt/observer/venv/bin/python /opt/observer/main.py

# Alternative: Using system Python (not recommended)
# ExecStart=/usr/bin/python3 /opt/observer/main.py

# -----------------------------------------------------------------------------
# Restart Policy
# -----------------------------------------------------------------------------
Restart=always
RestartSec=10

# Maximum restart attempts (5 times in 60 seconds = give up)
StartLimitIntervalSec=60
StartLimitBurst=5

# -----------------------------------------------------------------------------
# Timeouts
# -----------------------------------------------------------------------------
# Wait 30 seconds for startup
TimeoutStartSec=30

# Wait 30 seconds for graceful shutdown
TimeoutStopSec=30

# Send SIGINT for graceful shutdown, then SIGKILL
KillSignal=SIGINT
FinalKillSignal=SIGKILL

# -----------------------------------------------------------------------------
# Resource Limits
# -----------------------------------------------------------------------------
# Maximum open files (for many RSS connections)
LimitNOFILE=65535

# Maximum memory (adjust based on your system)
# MemoryMax=2G

# CPU quota (optional, percentage of one CPU core)
# CPUQuota=100%

# -----------------------------------------------------------------------------
# Security Hardening
# -----------------------------------------------------------------------------
# Protect system directories
ProtectSystem=strict
ProtectHome=true

# Allow write only to specific directories
ReadWritePaths=/opt/observer/data /opt/observer/logs

# Private /tmp
PrivateTmp=true

# No new privileges
NoNewPrivileges=true

# Restrict capabilities
CapabilityBoundingSet=
AmbientCapabilities=

# Restrict address families (only IPv4/IPv6 and Unix sockets)
RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX

# Restrict namespaces
RestrictNamespaces=true

# Restrict realtime scheduling
RestrictRealtime=true

# System call filter (allow common system calls only)
SystemCallFilter=@system-service
SystemCallFilter=~@privileged @resources

# Protect kernel tunables
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectKernelLogs=true

# Protect control groups
ProtectControlGroups=true

# Private devices
PrivateDevices=true

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
StandardOutput=journal
StandardError=journal
SyslogIdentifier=observer

[Install]
WantedBy=multi-user.target
